@model List<E_commerce.DTOs.ProductListItemDto>

@{
    ViewData["Title"] = "Home";
}


<div class="container py-5">

    <h2 class="mb-4">Products</h2>

    @* <div class="row g-4">
        @foreach (var product in Model)
        {
            <partial name="_ProductCard" model="product" />
        }
    </div> *@

</div>